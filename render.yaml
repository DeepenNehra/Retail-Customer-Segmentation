services:
  # Backend API
  - type: web
    name: segmentation-knight-backend
    runtime: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn backend.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: segmentation-knight-db
          property: connectionString
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
    healthCheckPath: /api/

  # Frontend
  - type: web
    name: segmentation-knight-frontend
    runtime: node
    rootDir: frontend
    buildCommand: "npm install && npm run build && npm install -g serve"
    startCommand: "serve -s dist -p $PORT"
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: VITE_API_URL
        sync: false

# Database
databases:
  - name: segmentation-knight-db
    databaseName: segmentation_knight
    user: segmentation_knight_user
